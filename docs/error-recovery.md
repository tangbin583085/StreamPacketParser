# 错误恢复

损坏数据属于通信过程中的正常情况，不应该直接让解析器停止。

遇到非法长度或校验失败时，解析器会生成一个诊断，然后从当前候选帧头后一个字节继续搜索。这样即使错误数据中间夹着下一条正常帧，也有机会重新同步。自定义校验器如果抛出异常，也会被记录为诊断，不会把异常继续抛到传输回调之外。

开发者配置错误会在创建 `PacketParserOptions` 时尽早抛出，例如空帧头、长度字段大小不支持、最大值小于最小值等。解析过程中的长度计算使用checked运算，内部缓存也有明确上限。
